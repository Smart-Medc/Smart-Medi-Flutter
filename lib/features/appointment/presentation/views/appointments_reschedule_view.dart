import 'package:flutter/material.dart';

class AppointmentsRescheduleView extends StatelessWidget {
  const AppointmentsRescheduleView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
